//! CLI command implementations: `start` (server/start), `status`, `stop`,
//! and `doctor`.
//!
//! Populated by the US2 tasks (T067-T074); the `status`/`stop` commands are
//! compiled out entirely when the `api` feature is off.
